// This screen has been disabled — OTP login via phone has been removed.
// The router no longer registers this screen.
// Kept as a stub to avoid dangling file issues.
import 'package:flutter/material.dart';

class MobileInputScreen extends StatelessWidget {
  const MobileInputScreen({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
