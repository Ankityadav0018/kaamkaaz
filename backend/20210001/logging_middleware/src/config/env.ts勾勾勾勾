import dotenv from 'dotenv';
dotenv.config();

export const config = {
  LOG_SERVER_URL: process.env.LOG_SERVER_URL || 'https://api.logserver.com',
  AUTH_EMAIL: process.env.LOG_AUTH_EMAIL || '',
  AUTH_PASSWORD: process.env.LOG_AUTH_PASSWORD || '',
  APP_ENV: process.env.NODE_ENV || 'development',
};
